# Database
DATABASE_URL=postgresql://user:pass@localhost:5432/darvinyi_latitude_dev

# Auth
JWT_SECRET=replace-me-with-long-random-string-min-32-chars
COOKIE_DOMAIN=localhost
NODE_ENV=development

# API key encryption (32-byte base64 — generate with: openssl rand -base64 32)
API_KEY_ENCRYPTION_KEY=replace-me-32-byte-base64-key

# External services
MAPBOX_TOKEN=pk.your-public-token-from-mapbox-com

# Server
PORT=3000
CLIENT_ORIGIN=http://localhost:5173
